# macOS
.DS_Store

# Python caches and build artifacts
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
*.egg-info/

# Local session/planning artifacts
.sisyphus/
session-ses_*.md
firebase-debug.log

# Legacy/reference datasets and papers kept out of git (root-level only)
/*.pdf
/*.pptx
/*.bib
/*.zip
/*.png
/*.jpg

# Legacy extracted directories (root-level)
/CameraLensArray/
/DeconvolutionColorPrior/
/fresnel_lens/

# Scratch outputs (root-level early-dev renders)
/outputs/

# Blender backup files
*.blend1

# Large binary artifacts that are reproducible via project scripts
# (keep .blend scenes, .glb models, PNGs, and metadata in git;
#  exclude heavy intermediates that can be regenerated)
project/assets/lenses/*/step4/*.exr
project/assets/lenses/*/step4/*.npy
project/assets/lenses/*/step5_step6/*.npz
project/assets/lenses/*/step5_step6/*.npy
